name = "test-hot-app"
main = "new-worker.js"
compatibility_date = "2023-10-16"

# 静态资源配置
[site]
bucket = "./"

# 环境变量
[vars]
ENVIRONMENT = "production"

# 静态资源配置 - 更新配置方式
[site]
include = [
  "*.html",
  "*.css",
  "*.js",
  "*.json",
  "*.png",
  "*.jpg",
  "*.gif",
  "*.svg",
  "*.ico"
]
exclude = [
  "server/node_modules/**/*",
  ".git/**/*",
  "node_modules/**/*",
  "wrangler.toml",
  "package.json",
  "package-lock.json"
]

# 修改入口点配置
[build]
command = ""
watch_dir = ["server", "."]

# 开发配置
[dev]
port = 8787
local_protocol = "http" 